[build]
builder = "NIXPACKS"
buildCommand = "cd api && npm ci && npm run build"

[deploy]
startCommand = "cd api && npm run start:railway"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
